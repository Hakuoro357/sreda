# DEPRECATED — see GitHub Issues

> **2026-05-10:** этот файл больше не source-of-truth для планирования.
> Roadmap-first restructure перенёс работу в **GitHub Project #2 «Среда»**
> (https://github.com/users/Hakuoro357/projects/2).
>
> ## Где искать что:
>
> | Что было здесь | Где теперь |
> |---|---|
> | 6 эпиков на Cycle 1+2 | Issues #1-#6 (label `epic`) |
> | Site execution (S-1a..S-10) | Issues #7-#18 (Epic #1 sub-issues) |
> | Reliability hard-commits (R-3 streaming, R-4 weather retry) | Issues #19-#20 (Epic #3) |
> | Onboarding near-term (O-1 lazy-provision, O-2 tour metrics) | Issues #21-#22 (Epic #2) |
> | Triage stale items (36 items deleted) | `docs/project-restart-triage-2026-05-10.md` |
>
> ## Top-5 миграция из этого файла:
>
> 1. **Reminder dedup race (R-1)** → создать как child issue Epic #3 (Reliability) когда дойдём (Iter 3-4).
> 2. **Mobile app planning** → `plans/sredaspace-mobile-app-draft-r3.md` (local), Epic #6 (Cycle 2).
> 3. **Site execution** → созданы Issues #7-#18 как Site sub-issues.
> 4. **Voice TTS responses** → Mobile Cycle 2 backlog (M-future placeholder в Epic #6).
> 5. **Local-first encrypted storage** → C-A1 в Epic #4 (Compliance Design) + Epic #5 (Impl Cycle 2).
>
> ## Что осталось в этом файле:
>
> Историческая запись в read-only режиме. Новые задачи **НЕ** добавлять
> сюда — открывать GitHub Issue с правильным label и Project #2 fields.
>
> Hot-fix candidates вне roadmap (см. triage doc):
> - Channel-linking rate limit restore (verify on prod)
> - Provider content-filter English leaks
> - Checklist duplicate on single-turn create+add
>
> Daily ops cadence: см. issue templates в `.github/ISSUE_TEMPLATE/`.

---


> **Ротация (2026-06-12):** держим последние ~3 дня EOD-записей; более
> старое — в `docs/archive/tomorrow-plan-archive.md`.

# EOD 2026-06-11 → план на 2026-06-12

## Сделано сегодня (2026-06-11)

- ✅ **#127 закрыт и на проде** (sreda d291302): ретеншн снова работает —
  FK-развязка inbound+jobs (jobs-ветку нашёл калибровочный субагент —
  CRITICAL, оба Codex пропустили; зонд: 556/557 пар), воркер коммитит сам
  (R1 CRITICAL: flush-без-commit — чистка могла НИКОГДА не персиститься),
  анти-шторм (откат 30 мин + P1 после 3), индекс + миграция 20260611_0055.
  Первый прогон вычистил 4681 строку, контроль чистый. Ревью R1→R4.
- ✅ Архитектурные предложения обсуждены с владельцем → задачи #133
  (поведенческий CI), #134 (честность, тень), #135 (память планов).
- ✅ **План #133 сошёлся** (R5, оба NSC; 38 замечаний, 1 отклонено):
  полностью async-харнес, прод-швы (ContextVar планировщика, локальный
  _create_task), сеть-бан, матрица покрытия 12 семейств. Финал:
  `plans/133-functional-ci-final.md`. Попутно найден РЕАЛЬНЫЙ латентный
  баг диспетчера очереди → #136.
- ✅ Аудит готовности к широкому запуску → решения владельца: открытая
  бесплатная альфа, 200–300 мест, платёжный код ждёт, изоляция и
  анти-инъекции на уровне механизма. Веха №1 «Широкий запуск» (14 задач),
  новые #137 (сайт + правовой блок), #138 (RLS-изоляция).
- ✅ Программа «Надёжность диалога» —
  `plans/dialog-reliability-programme.md` (этапы: мерило → ворота #133 →
  классы дефектов #124/#125/#122/#106/#110 → честность #134 → петля).
- ✅ Спеки поправлены: ручного одобрения нет (Phase 2C) — gtm-plan.md +
  память (reference_onboarding_flow).

## План на 2026-06-12 (по приоритету)

1. **Дождаться решений владельца** (блокер старта):
   (а) чеклист приёмки #133 — топ-пункты в его формулировке или
   «утверждаю как есть»; (б) KPI запуска (≥95% ходов без провала / 14
   дней); (в) порядок этапа 2 (старт с #124).
   Приёмка: решения зафиксированы в issue #133 и в программе.
2. **#133 фаза A — каркас + пилоты** (после утверждения чеклиста):
   PlanExecuteHarness, прод-швы (red-first), 5 пилотных сценариев.
   Приёмка: пилоты зелёные локально, фаза A < ~20 сек, цикл ревью кода
   пройден. Оценка: ~1 день.
3. **Этап 0 программы надёжности — мерило**: ежедневный автоотчёт
   (ходы/провалы по классам) в админ-чат. Приёмка: первая сводка пришла
   за реальные сутки. Оценка: ~0.5 дня. Завести issue (ПРАВИЛО #5).
4. **#35 — разбор 3 ссылок владельца** (Plan-and-Solve, ReWOO, to-do
   middleware) как вход в дизайн #124. Приёмка: краткий разбор «что
   берём в Среду» в чате. Оценка: ~1 час.
5. Хвост: проверить лог sreda.retention за сутки (чистый? счётчики
   нулевые после бэклога) — закрепление #127.

## Блокеры / ждём владельца

- Чеклист #133, KPI, порядок этапа 2 (п.1 выше).
- #137 (сайт): передача макетов дизайна.

## Не закоммичено сознательно (sreda, публичный реп)

- `scripts/dev/live_115_smoke.py`, `scripts/replay/diag_replies.py`,
  `scripts/replay/export_archive.py` — содержат реальные ID юзеров,
  в ПУБЛИЧНЫЙ sreda нельзя (правило); держим локально до обезличивания.
- `deploy/sredaspace/static/logo_640x360.png` — артефакт прошлой сессии
  (пост в ТГ), назначение в деплое не подтверждено.

---

# EOD 2026-06-12 → план на 2026-06-13 (автономный день — делегированная цель «подготовка к дебюту»)

## Сделано сегодня (на проде, каждый срез — тройное ревью до сходимости)
- ✅ **#139** суточная сводка надёжности (этап 0 программы) — закрыт, деплой; первая сводка в МАКС (9/9 чисто).
- ✅ **#133 фаза A** функциональные ворота-тесты тракта (12 тестов, прод-швы, харнес) — на проде.
- ✅ **#135 срез 1** память планов (сбор обезличенных форм + тень) — на проде ТОЛЬКО на тенанте Бориса (tenant_max_40921122); рендер в промпт (срез 4) ждёт команды владельца.
- ✅ **#95** токены из логов (редактирующий форматтер + вычистка 162k историч. строк) — закрыт.
- ✅ **#124 срез 1** блок ПОСЛЕДНИЕ_РЕПЛИКИ_ЮЗЕРА (детерминированный источник — корень провала 9/9) — на проде, все тенанты.
- ✅ **#105** off-peak time-bomb в credit-тестах (g-047) — набор стабилен.
- ✅ **#124 срез 2 ч.1** матчер анти-фабрикации (item_matcher.py) — СОШЁЛСЯ, в main, НЕ задеплоен (нулевое поведение, гейт не подключён).
- ✅ Планы сошлись: #124 (R3), #134 честность (R4), #135 (R4). Разбор 3 ссылок владельца → в дизайн #135.
- ✅ Калибровка: субагент дал 7 уникальных CRITICAL/MAJOR за день.

## План на 2026-06-13 (приоритет — надёжность диалога, потом блокеры запуска)
1. **#124 срез 2 ч.2 — гейт в validate_plan** (ВЫСОКИЙ blast-radius — гейтит все записи; строить особо тщательно red-first, отдельный полный тройной цикл). Дизайн зафиксирован в plans/133-phase-a-progress.md (конец файла): флаг ToolSpec adds_user_items + per-tool item-поле + реестровый тест; collect_sources (current msg + блок реплик) в validate_plan; literal item → item_grounded_in_sources иначе Violation fabricated_item_suspected → retry; ref → provenance (source-approved read-tool). РЕГРЕССИЯ: «добавь молоко и хлеб» одним сообщением не должен ломаться.
   Приёмка: e2e 10 пунктов точно + 11-й выдуманный отклонён + #133-сценарий write-intent.
2. **#124 срез 3** few-shot «сбор из истории» (формат, зеркалящий реальный блок) + policy-строка; бюджет префикса.
3. **#124 живой прогон** на тестовом аккаунте 352612382 (g-044) → решение о срезе 4 (collector-tool).
4. **#134 честность ответа** — код детерминированной сверки + ИИ-судья, запуск ТЕНЕВОГО режима (неделя лога без смены поведения). Чем раньше тень — тем раньше ворота.
5. Параллельно (блокеры запуска, можно межевать): **#138** изоляция семей (RLS), **#95→** ничего (закрыто), **#89/#83** инъекции, **#102** mini-app Android.

## Ждёт владельца (накопилось)
- Ротация токена @sreda01_bot через BotFather (лежал в логах с 3 июня; на диске вычищен).
- Макеты дизайна сайта для #137 (блокер воронки).
- Решение включить подмешивание примеров #135 срез 4 (после просмотра накопленного куратором).
- KPI/порядок этапа 2 уже утверждены под делегированной автономией (зафиксированы в issues).

## Метрика дебюта
KPI ≥95% ходов без провала за 14 дней — счётчик пошёл 2026-06-12 (день 1: 9/9). Критический путь: теневая неделя #134 + 14 дней наблюдения → реалистичный дебют конец июня / начало июля.

## Не закоммичено сознательно (публичный sreda)
scripts/dev/live_115_smoke.py, scripts/replay/{diag_replies,export_archive}.py (реальные ID юзеров), deploy/sredaspace/static/logo_640x360.png.

---

# EOD 2026-06-14 → план на следующий день (надёжность диалога — приоритет №1)

## Сделано сегодня (2 среза на прод, оба тройное ревью до сходимости)
- ✅ **#141** композер прятал УСПЕШНЫЕ операции за «поломкой» (presenter deny-by-default: инструмент отработал, но нет правила показа → «я сломалась»). Закрыты ВСЕ 47 success-пар (display_summary/raw_text/override, id скрыты, порядок ввода). Ревью R1→R4 сошлось (я + Codex high + medium + Claude-субагент, гонявший код). Деплой на тенант Бориса (`ab071b1`), smoke зелёный. Тест полноты реестра — анти-регресс.
- ✅ **#140** проба `failed_turns_rate` считала провалом `iters==0` — у планировщика iters ВСЕГДА 0 даже на успехе → ложные CRITICAL + KPI ≥95% недостоверен. Фикс: `trace.outcome`='breakdown' (ставится в единой точке `note_breakdown`), проба считает по нему; outcome переживает serialize/deserialize для outbox-воркера (Codex CRITICAL). Ревью R1→R2 сошлось. Деплой (`f581fa5`), smoke.
- ✅ **#133 eval** — сквозной прогон по семействам с прод-эмбеддингами/конфигом на VDS, изоляция наблюдаемости (НЕ пачкает прод-лог/алерты), захват `plan_json` через обёртку `execute_plan`. Прогон подтвердил эффект #141 (presenter-класс починен).
- ✅ **Триаж прогона #133** → issues: #142 (меню header-only), #143 (операции над объектом по описанию), #144 (меню создать/изменить — invalid_plan), #145 (web_search в реестре, но не в executor + валидатор не гейтит).
- ✅ **План #143 сошёлся** — НЕ resolver-слой: finder (`find_by_title`, чек-лист self-resolve) уже есть; корни — имя поля `items`→`tasks` + нет few-shot задач + спека mark вводит в заблуждение + finder молча берёт первое. Зафиксирован в `plans/2026-06-14-capability-map-and-143-plan.md`, D2 включён (minimal-destructive). Урок: Codex план-ревью кода не видит → советовал строить готовое; поймал Борис.
- ✅ **План карты возможностей** (внутр. тулинг, `scripts/dev/gen_capabilities.py`) — утверждён **lean v1** после Codex high+medium план-ревью. Чтобы не переизобретать существующие механизмы.
- ⚠️ **Инцидент:** стенд #133 слал P1-алерты в личку владельца (через MAX) + ложный CRITICAL (загрязнял trace.log) — изоляция наблюдаемости доработана (перехват сетевых примитивов алертов + зануление лог-путей).

## План на следующий день (по приоритету)
1. **Карта возможностей #52** — построить `gen_capabilities.py` lean-v1 по утверждённому плану. Приёмка: golden-тест ключевых строк (`complete_task=id_only`, `mark=self_resolves_title`, `find_by_title` присутствует), `--check` зелёный.
2. **#143** реализовать по зафиксированному плану (A1 имя поля + A-доп валидатор + B few-shot + C спека mark + D2 безопасность). Приёмка: «отмени про интернет» — отменена/не поломка; 2+ → переспрос; «отметь лопату» — один шаг; валидатор ловит `items`-у-задач; живой прогон N=5 (g-044). Ревью я+Codex high+medium.
3. **#144** меню создать/изменить (планировщик не строит валидный план).
4. **#124 срез-2 ч.2** гейт (если не покрыт #143) + срез-3 few-shot.

## Ждёт владельца
- Ротация токена @sreda01_bot через BotFather.
- Макеты дизайна сайта #137.
- Решение по #135 срез 4 (подмешивание примеров).

## Метрика дебюта
KPI ≥95% ходов без провала за 14 дней. После #140 проба считает ЧЕСТНО (по outcome, не iters) — критично для запуска на планировщик-трафике. Реалистичный дебют конец июня / начало июля.
