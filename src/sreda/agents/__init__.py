"""R-39 агенты — полу-шаблонный поток разговора.

Содержит:

- ``contracts`` — ``ToolContract``, ``ToolJournalEntry``, ``TurnContext``
  и реестр контрактов на ключевые пишущие инструменты.

Этот пакет — точка входа для нового механизма гибрида (полу-шаблон +
живая фраза). Старый стек продолжает работать параллельно до пилота
по `runtime_config.r39_pilot_tenant_ids`.
"""
